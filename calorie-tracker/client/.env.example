# Only needed for production deploys where the client and server are hosted
# separately. Leave unset for local development (Vite's dev proxy handles it).
VITE_API_URL=https://your-server-url.onrender.com
